[project]
name = "connect-4"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.4",
    "pytest>=9.0.2",
]

[dependency-groups]
dev = [
    "black>=26.3.1",
]
